# Pinned to the exact versions this bot was built and tested against
# (503 tests green). Pinning matters here: with open `>=` ranges, a future
# `docker compose build` could pull a breaking major of aiogram / anthropic
# / openai / pydantic and the bot would fail to start with no maintainer
# around to fix it. To upgrade deliberately: bump a pin, rebuild, run the
# test suite (`python -m pytest`), and only then deploy.
aiogram==3.13.1
openai==2.44.0
anthropic==0.112.0
asyncpg==0.31.0
pgvector==0.4.2
tiktoken==0.13.0
pydantic==2.9.2
pydantic-settings==2.14.2
python-dotenv==1.2.2
httpx==0.28.1
tenacity==9.1.4
numpy==2.4.6
scipy==1.17.1
websockets==16.0
captcha==0.7.1
